using System;

using ServerLibrary.Model;
using ServerLibrary.Utils;

using WebPortal.App_LocalResources;

namespace WebPortal.UIModel
{
    public class UIHomeInspectionList
    {
        /*
        private const int MAX_CUSTOMER_LENGTH    = 22;
        private const int MAX_RESPONSIBLE_LENGTH = 23;

        public string customer    { get; set; }
        public string responsible { get; set; }
        public string quicklink   { get; set; }

        public UIHomeInspectionList(Inspection inspection, Customer customer, Account responsible)
        {
            this.customer    = (customer == null) ? Strings.None : StringUtils.Shorten(customer.name, MAX_CUSTOMER_LENGTH);
            this.responsible = (responsible == null) ? Strings.None : StringUtils.Shorten(responsible.Name, MAX_RESPONSIBLE_LENGTH);
            this.quicklink   = "/Inspection/Index/" + inspection.id;
        }
         */
    }
}